# 智能多平台求职与简历投递系统

> 项目规划、产品定义与技术实施方案
>
> 文档版本：v1.1
>
> 更新日期：2026-08-30

> 文档状态：立项与 MVP 规划稿

> 重要说明：平台开放接口、用户协议和风控规则可能随时变化。本文对各平台的能力只作产品规划依据，正式开发前必须以平台当前公开文档和书面合作结果为准。

## 快速决策摘要

如果现在就开始开发，建议采用以下默认决策：

| 决策项 | 默认选择 | 原因 |
|---|---|---|
| 首个产品形态 | AI 简历工作台 + 智能辅助投递 | 能快速验证需求，降低平台封禁和合规风险 |
| 首个自动化端 | Windows 桌面端 + Chrome/Edge 插件 | 桌面浏览器最适合做表单辅助填充 |
| 移动端定位 | 查看、筛选、确认、通知和进度管理 | iOS/Android 不适合后台控制第三方招聘 App |
| 首批职位来源 | 用户导入职位链接或授权数据源 | 不依赖未经授权的全量爬取 |
| 最终投递规则 | 默认由用户确认 | 防止误投、重复投递和未经同意的外部操作 |
| 后端默认方案 | NestJS + Python + PostgreSQL + Redis | 工程生态成熟，适合业务服务与 AI 异步任务 |
| 自动投递开放条件 | 仅官方接口或书面授权平台 | 保证稳定性、可解释性和合规边界 |

下面的详细章节可以直接作为产品需求、技术设计和开发排期的共同基线。

## 1. 项目结论

这是一个有真实需求、具备产品价值，但平台接入和合规风险较高的项目。

项目不应该简单定位成“批量群发简历工具”，而应该定位为：

> **AI 简历优化 + 多平台职位聚合 + 智能岗位匹配 + 自动填表 + 用户确认投递的求职工作台。**

从技术角度，以下能力可以实现：

- 简历上传、解析和结构化；
- AI 简历优化和岗位定制；
- 地区、薪资、岗位类型等条件筛选；
- 多平台职位聚合、清洗和去重；
- 岗位与简历的匹配评分；
- 自动生成求职话术和常见问题答案；
- 浏览器端自动填充简历信息；
- 投递状态、面试和求职进度管理；
- Web、Windows/macOS、Android/iOS 多端使用。

需要谨慎承诺的能力：

- 六大招聘平台同时提供官方候选人投递 API；
- 完全无人值守地批量点击最终投递；
- 绕过验证码、短信验证或平台风控；
- 长期稳定抓取所有平台的完整职位数据。

因此，建议产品采用分级投递模式：

1. **手动投递**：推荐职位并跳转平台，由用户操作。
2. **智能辅助投递**：系统自动填表，用户确认最终投递。
3. **授权自动投递**：仅对获得平台正式接口或书面授权的渠道开放。

## 2. 项目价值判断

### 2.1 市场需求

求职者普遍存在以下问题：

- 需要在多个招聘平台重复维护资料；
- 职位数量很多，但难以判断是否真正匹配；
- 同一份简历无法适应不同岗位；
- 反复填写教育经历、工作经历和附加问题；
- 投递后无法统一追踪进度；
- 批量投递数量高，但有效回复率低。

本项目如果能够减少无效投递、提高岗位匹配度并管理完整的求职流程，就具备明显价值。

### 2.2 项目优势

- Flutter 可以统一 Web、桌面端和移动端的产品体验；
- 简历解析和 AI 优化容易形成高频使用场景；
- 工作台模式能够承载职位筛选、简历管理和投递记录；
- 支持多份岗位定制简历，具有较好的差异化空间；
- 投递记录和面试跟踪可以提高用户留存；
- 后续可以扩展企业招聘、猎头协同和校园招聘场景。

### 2.3 最大挑战

- 国内招聘平台通常不会默认开放个人候选人批量投递接口；
- 页面结构、验证码和风控策略会不断变化；
- 不同平台的职位字段、薪资格式和投递流程不统一；
- 简历属于个人信息，包含联系方式、教育经历和工作经历；
- 全自动投递容易出现误投、重复投递和低质量群发；
- 仅以“投递数量”作为指标，会损害产品口碑。

### 2.4 总体判断

项目值得做，但不建议一开始就投入大量资源开发“六个平台全自动群投”。

最合理的路径是先验证：

1. 用户是否愿意上传简历并使用 AI 优化；
2. 岗位匹配结果是否比用户手动筛选更准确；
3. 自动填表是否真正节省时间；
4. 用户是否愿意持续记录和管理投递结果；
5. 在不违反平台规则的情况下，哪些平台可以稳定接入。

## 3. 产品定位

### 3.1 面向用户

第一阶段目标用户：

- 应届毕业生；
- 互联网和技术岗位求职者；
- 教师、运营、销售、行政等高频求职人群；
- 正在集中求职、需要同时维护多个招聘平台的用户；
- 需要中英文或多岗位简历的专业人士。

第二阶段可以扩展：

- 猎头顾问；
- 职业咨询机构；
- 高校就业中心；
- 企业内部转岗用户；
- 招聘服务机构。

### 3.2 产品口号建议

推荐对外表述：

> **让每一次投递都更匹配，而不是只投得更多。**

避免直接宣传：

> “绕过风控、无限群发、百分百自动投递。”

### 3.3 非目标

以下内容不属于第一阶段目标：

- 绕过验证码或平台安全策略；
- 代替用户参加在线测评、视频面试或语音面试；
- 虚构学历、证书、工作经历和项目成果；
- 在未取得授权的情况下复制并长期存储平台全部职位数据；
- 保存用户招聘平台密码；
- 通过共享账号批量操作。

## 4. 核心用户流程

```text
注册登录
   ↓
上传简历
   ↓
解析和校对
   ↓
AI 优化
   ↓
生成多个简历版本
   ↓
设置求职偏好
   ↓
同步或导入职位
   ↓
岗位匹配、筛选和去重
   ↓
选择职位和简历版本
   ↓
生成投递话术与问题答案
   ↓
浏览器自动填表
   ↓
用户检查并确认投递
   ↓
记录投递结果
   ↓
跟踪回复和面试进度
```

## 5. 功能需求

### 5.1 用户和账号

功能编号：`FR-AUTH`

- 手机号、邮箱或第三方账号登录；
- 多设备登录管理；
- 用户资料维护；
- 账号注销；
- 用户数据导出；
- 用户数据删除；
- 隐私授权记录；
- 会员和额度管理。

### 5.2 简历管理

功能编号：`FR-RESUME`

- 支持 PDF、Word、图片和文本格式；
- 上传进度和失败重试；
- OCR 和文档解析；
- 姓名、联系方式、教育经历、工作经历、项目经历和技能识别；
- 原始简历只读保存；
- 支持多个简历版本；
- 支持在线预览和下载；
- 支持简历删除和授权撤回；
- 支持中英文简历。

### 5.3 AI 简历优化

功能编号：`FR-AI`

- 简历完整度检查；
- 错别字和格式检查；
- 工作经历表达优化；
- 岗位关键词提取；
- 简历与职位匹配分析；
- 根据职位生成定制简历；
- 生成求职话术；
- 生成常见问题的回答建议；
- 展示每一处修改前后的差异；
- 禁止模型未经用户确认编造事实。

### 5.4 求职偏好

功能编号：`FR-PREFERENCE`

- 目标城市；
- 目标岗位；
- 期望薪资；
- 工作经验；
- 学历要求；
- 行业和公司规模；
- 接受或排除关键词；
- 是否接受远程；
- 是否接受出差；
- 是否接受加班；
- 到岗时间；
- 职位发布时间范围。

### 5.5 工作台和职位筛选

功能编号：`FR-WORKBENCH`

- 平台开关；
- 多条件组合筛选；
- 职位列表和详情；
- 匹配度评分；
- 匹配原因解释；
- 职位收藏；
- 职位忽略；
- 相似职位去重；
- 已投递职位标记；
- 职位下架检测；
- 原始职位链接跳转；
- 职位批量选择。

### 5.6 投递管理

功能编号：`FR-APPLICATION`

- 选择简历版本；
- 选择求职话术；
- 预览将要发送的内容；
- 创建投递任务；
- 投递前重复检查；
- 用户确认；
- 自动填表；
- 验证码时暂停；
- 任务取消和暂停；
- 投递结果记录；
- 失败原因记录；
- 面试阶段管理；
- 导出投递报表。

### 5.7 通知和提醒

功能编号：`FR-NOTIFICATION`

- 投递成功通知；
- 投递失败通知；
- 需要补充信息通知；
- 验证码通知；
- 面试时间提醒；
- 跟进提醒；
- App 推送、邮件和浏览器通知。

## 6. 平台接入策略

### 6.1 接入等级

每个平台都应该记录自己的能力，而不是用一个简单的“已接入”字段。

```text
L0：仅保存外部职位链接
L1：读取用户当前打开的职位页面
L2：职位解析和智能填表
L3：用户确认后辅助投递
L4：官方 API 投递和状态同步
```

### 6.2 六个平台的初步策略

| 平台 | 第一阶段建议 | 目标等级 | 说明 |
|---|---|---:|---|
| BOSS 直聘 | 用户当前页面解析、辅助填表 | L2-L3 | 需要重点验证页面结构、聊天流程和风控 |
| 智联招聘 | 职位链接导入、辅助投递 | L2-L3 | 需要适配附加问题和外部网申入口 |
| 猎聘 | 职位解析、简历版本选择 | L2-L3 | 需要处理猎头沟通和职位跳转 |
| 前程无忧 | 聚合职位、跳转或辅助投递 | L1-L3 | 部分职位会进入企业自建招聘系统 |
| 鱼泡直聘 | 单独验证网页或 App 流程 | L1-L3 | 不在首个版本承诺全自动 |
| 国聘 | 优先使用公开入口或合作接口 | L1-L4 | 政企招聘流程和实名信息需要谨慎处理 |

### 6.3 平台适配器接口

建议建立统一接口：

```dart
abstract class JobPlatformAdapter {
  String get platformCode;

  PlatformCapabilities get capabilities;

  Future<List<Job>> searchJobs(JobSearchQuery query);

  Future<JobDetail> getJobDetail(String externalJobId);

  Future<ApplicationPreparation> prepareApplication(
    JobDetail job,
    ResumeVersion resume,
  );

  Future<ApplicationResult> submitApplication(
    ApplicationRequest request,
  );

  Future<ApplicationStatus> getApplicationStatus(
    String externalApplicationId,
  );
}
```

未获得官方授权的平台，`submitApplication` 不应直接在云端代替用户执行，而应交由浏览器插件完成辅助填表，最终由用户确认。

## 7. 跨平台实现方案

### 7.1 Flutter Web

适合实现：

- 工作台；
- 简历管理；
- 职位筛选；
- 投递记录；
- 统计报表；
- 账号和隐私设置。

不应依赖 Flutter Web 直接控制其他网站页面。浏览器的跨域和安全策略会限制这种做法。

推荐搭配 Chrome/Edge 浏览器插件：

```text
Flutter Web 工作台
        ↓
浏览器扩展
        ↓
用户当前已经登录的招聘平台网页
```

### 7.2 Windows/macOS 桌面端

桌面端是最适合做智能辅助投递的版本。

推荐结构：

```text
Flutter Desktop
        ↓ WebSocket / 本地 HTTP
本地 Automation Agent
        ↓
Chrome/Edge Extension
        ↓
招聘平台网页
```

桌面端负责：

- 工作台和任务管理；
- 本地文件选择；
- 自动化权限管理；
- 任务暂停和恢复；
- 浏览器连接状态；
- 系统托盘通知。

本地自动化助手负责：

- 与浏览器插件通信；
- 执行页面元素识别；
- 自动填充表单；
- 上传简历附件；
- 检测登录失效和验证码；
- 返回成功、失败和异常信息。

### 7.3 Android/iOS

移动端主要负责：

- 查看职位；
- 简历编辑；
- 岗位筛选；
- 投递确认；
- 接收通知；
- 处理验证码和开放性问题；
- 跟踪面试进度。

不建议让移动端后台控制其他招聘 App。推荐采用：

- 深度链接；
- 系统分享；
- 打开原始职位页面；
- 复制求职话术；
- 用户在原平台完成最终操作；
- 回到本系统确认投递结果。

## 8. 后端架构

### 8.1 服务模块

```text
API Gateway
├── Auth Service                 用户、登录、权限
├── Resume Service               简历上传和版本管理
├── Resume Parser                OCR、PDF、Word 解析
├── AI Service                   简历优化和岗位匹配
├── Job Service                  职位同步、标准化和去重
├── Matching Service             匹配度计算
├── Application Service          投递任务和状态机
├── Platform Connector Service   平台适配器
├── Notification Service         通知和提醒
├── Billing Service              会员和额度
└── Audit Service                审计和隐私记录
```

### 8.2 推荐技术栈

```text
客户端：Flutter / Dart
业务 API：NestJS 或 Go
AI 和解析：Python / FastAPI
数据库：PostgreSQL
缓存和队列：Redis + BullMQ 或 Redis Streams
文件存储：MinIO、OSS 或 COS
搜索：OpenSearch 或 Elasticsearch
实时通信：WebSocket 或 SSE
浏览器自动化：Chrome/Edge Extension + Playwright
部署：Docker + Nginx + CDN
监控：结构化日志、错误追踪、任务指标监控
```

## 9. 核心数据模型

### 9.1 用户和授权

```text
users
user_devices
user_preferences
user_consents
subscriptions
```

### 9.2 简历

```text
resumes
resume_versions
resume_sections
resume_parse_tasks
resume_optimize_tasks
```

### 9.3 职位

```text
job_platforms
jobs
job_snapshots
job_tags
job_search_profiles
job_match_scores
```

### 9.4 投递

```text
applications
application_attempts
application_answers
application_events
application_status_history
```

### 9.5 自动化和审计

```text
browser_sessions
automation_tasks
automation_task_events
platform_capabilities
audit_logs
data_access_logs
```

投递去重至少需要使用：

```text
(user_id, platform_code, external_job_id)
```

当平台职位 ID 不稳定时，再使用公司、职位、城市和链接生成辅助指纹。

## 10. 投递任务状态机

```text
CREATED
  ↓
WAITING_APPROVAL
  ↓
QUEUED
  ↓
OPENING_PLATFORM
  ↓
FILLING_FORM
  ↓
WAITING_USER_CONFIRM
  ↓
SUBMITTING
  ↓
SUBMITTED
  ↓
STATUS_TRACKING
```

异常状态：

```text
DUPLICATED          重复职位
JOB_EXPIRED         职位已下架
LOGIN_REQUIRED      需要登录
CAPTCHA_REQUIRED    需要验证码
FORM_CHANGED        表单结构变化
RATE_LIMITED        平台限流
PLATFORM_BLOCKED    平台限制
USER_CANCELLED      用户取消
FAILED              其他失败
```

检测到验证码、实名验证或页面结构变化时，任务必须暂停，不应无限重试。

## 11. 简历 AI 处理流程

```text
原始文件上传
   ↓
文件安全检查
   ↓
OCR / 文档解析
   ↓
字段标准化
   ↓
用户校对
   ↓
目标岗位分析
   ↓
AI 优化建议
   ↓
用户确认
   ↓
生成新简历版本
```

每次 AI 修改都生成新版本，不能覆盖原始简历。

建议在界面中展示修改差异：

```text
原文：负责系统开发。

优化后：负责核心业务系统的架构设计与开发，推动接口响应时间降低 35%。
```

如果“降低 35%”不是用户原文中的事实，系统必须提示用户确认，不能自动当成真实经历。

## 12. API 设计示例

### 12.1 简历

```text
POST   /api/v1/resumes/upload-url
POST   /api/v1/resumes
GET    /api/v1/resumes
GET    /api/v1/resumes/:id
POST   /api/v1/resumes/:id/parse
POST   /api/v1/resumes/:id/optimize
GET    /api/v1/resumes/:id/versions
DELETE /api/v1/resumes/:id
```

### 12.2 职位

```text
POST   /api/v1/jobs/import-link
GET    /api/v1/jobs
GET    /api/v1/jobs/:id
POST   /api/v1/jobs/:id/favorite
POST   /api/v1/jobs/:id/ignore
POST   /api/v1/jobs/match
```

### 12.3 投递

```text
POST   /api/v1/applications/preview
POST   /api/v1/applications
GET    /api/v1/applications
GET    /api/v1/applications/:id
POST   /api/v1/applications/:id/approve
POST   /api/v1/applications/:id/cancel
POST   /api/v1/applications/:id/retry
PATCH  /api/v1/applications/:id/status
```

## 13. Flutter 页面规划

```text
启动页
登录页
首页
简历中心
  ├── 简历列表
  ├── 上传简历
  ├── 解析结果
  ├── AI 优化
  ├── 简历版本
  └── 简历预览
求职偏好
工作台
  ├── 职位筛选
  ├── 职位列表
  ├── 职位详情
  └── 批量选择
投递中心
  ├── 待确认
  ├── 已投递
  ├── 投递失败
  ├── 面试中
  └── 投递详情
消息中心
会员中心
账号设置
隐私与授权
```

桌面端建议使用“左侧导航 + 中间职位列表 + 右侧职位详情”的三栏布局；移动端使用底部导航、抽屉筛选和全屏职位详情。

## 14. 开发阶段规划

### 阶段 0：需求和合规确认

交付内容：

- 明确目标用户和首个岗位类型；
- 明确首个平台和投递模式；
- 梳理平台用户协议和接入要求；
- 确认简历数据的收集、处理和删除规则；
- 输出 UI 原型和接口草案；
- 确定“用户确认最终投递”的产品规则。

### 阶段 1：简历 MVP

交付内容：

- Flutter 登录；
- 简历上传；
- PDF/Word 解析；
- 结构化简历编辑；
- AI 优化；
- 多版本简历；
- 预览和导出；
- 基础隐私设置。

验收标准：

- 用户可以在 Web、桌面和手机端查看同一份简历；
- 原始简历不会被优化结果覆盖；
- 用户可以查看 AI 修改前后差异；
- 用户可以删除简历及其解析结果。

### 阶段 2：工作台 MVP

交付内容：

- 求职偏好设置；
- 职位链接导入；
- 职位字段标准化；
- 地区、薪资、岗位类型筛选；
- 岗位匹配度；
- 职位收藏和忽略；
- 职位去重；
- 投递记录。

验收标准：

- 用户能够按条件筛选职位；
- 同一职位不会反复出现在列表中；
- 每个职位显示匹配理由；
- 用户可以选择不同简历版本。

### 阶段 3：桌面智能辅助投递

交付内容：

- Chrome/Edge 插件；
- Flutter 桌面助手；
- 当前职位页面识别；
- 基础字段自动填充；
- 简历附件选择；
- 求职话术填充；
- 验证码检测和任务暂停；
- 用户最终确认。

验收标准：

- 不保存招聘平台密码；
- 用户可以随时暂停任务；
- 检测到验证码不会继续批量操作；
- 每次投递都有完整记录；
- 投递失败能够说明原因。

### 阶段 4：平台扩展和官方合作

交付内容：

- 逐个平台适配；
- 平台能力分级；
- 平台页面变化监控；
- 官方 API 或合作接口；
- 授权自动投递；
- 投递状态同步。

## 15. 安全与合规要求

简历中通常包含手机号、邮箱、教育经历、工作经历、居住地、照片和证书信息，属于个人信息；身份证号、健康信息等内容可能属于敏感个人信息。

必须做到：

- 全链路 HTTPS；
- 文件和数据库加密存储；
- 租户之间严格隔离；
- 最小化收集个人信息；
- 明确 AI 处理范围和第三方服务商；
- 默认不使用用户简历训练模型；
- 支持查看、导出、删除和撤回授权；
- 设置数据保留期限；
- 记录简历访问和投递操作；
- 不保存招聘平台密码；
- 不收集短信验证码；
- 不绕过 CAPTCHA、风控或实名验证；
- 投递前展示平台、公司、职位、简历和发送内容；
- 最终投递需要用户确认，除非平台提供明确授权接口。

需要在产品上线前准备：

- 用户协议；
- 隐私政策；
- AI 生成内容说明；
- 简历数据处理说明；
- 第三方平台授权说明；
- 数据删除和注销流程；
- 客服和投诉处理流程。

## 16. 测试方案

### 16.1 客户端测试

- Flutter 单元测试；
- Provider/状态管理测试；
- Widget 测试；
- Web、Windows、Android、iOS 真机测试；
- 不同屏幕尺寸适配测试；
- 离线和弱网测试；
- 文件上传失败重试测试。

### 16.2 后端测试

- API 单元测试；
- 数据库迁移测试；
- 队列任务测试；
- 幂等性测试；
- 重复投递测试；
- 权限隔离测试；
- 文件访问权限测试；
- 删除和撤回授权测试。

### 16.3 自动化测试

- 页面字段识别测试；
- 职位详情解析测试；
- 表单填充测试；
- 登录失效测试；
- 验证码检测测试；
- 页面结构变化测试；
- 投递成功提示识别测试；
- 任务暂停和恢复测试。

### 16.4 AI 测试

- 简历字段准确率；
- 岗位分类准确率；
- 匹配评分一致性；
- 幻觉和编造事实测试；
- 敏感信息泄露测试；
- 不同岗位模板质量测试。

## 17. 产品指标

不建议把“每天投递多少份”作为唯一指标。

核心指标建议包括：

```text
简历解析成功率
简历字段识别准确率
岗位匹配点击率
职位收藏率
投递完成率
重复投递拦截率
投递失败率
平台账号受限率
招聘者回复率
面试转化率
用户七日留存率
用户简历再次优化率
```

最重要的北极星指标可以定义为：

> **有效面试数 / 用户确认投递数**

这比单纯追求投递数量更能体现产品价值。

## 18. 商业模式建议

### 免费版

- 一份基础简历；
- 每月有限次 AI 优化；
- 基础职位筛选；
- 手动投递记录。

### 个人会员

- 多份简历版本；
- 岗位定制简历；
- 高级匹配分析；
- 智能求职话术；
- 浏览器智能填表；
- 投递数据统计。

### 高级会员

- 更多 AI 使用额度；
- 多平台工作台；
- 面试跟踪；
- 简历质量报告；
- 个人求职顾问式建议。

### 企业或机构版

- 高校就业中心；
- 职业咨询机构；
- 猎头团队；
- 多用户管理；
- 数据看板；
- 私有化部署；
- 企业招聘系统对接。

不建议仅按照“投递份数”收费，否则容易诱导低质量群发。

## 19. 主要风险和应对方案

| 风险 | 影响 | 应对方案 |
|---|---|---|
| 平台没有公开个人投递 API | 无法直接云端全自动投递 | 使用职位链接、浏览器插件和用户确认模式 |
| 页面结构变化 | 自动填表失效 | 平台适配器独立、增加页面变化监控和回归测试 |
| 验证码和风控 | 任务中断或账号受限 | 限速、检测后暂停、要求用户处理，不绕过风控 |
| 简历误投 | 用户投诉、信任下降 | 投递前预览、重复检查和最终确认 |
| AI 编造经历 | 诚信和法律风险 | 原文对比、事实确认和敏感内容拦截 |
| 简历泄露 | 严重隐私风险 | 加密、最小化访问、可删除、审计和供应商管理 |
| 职位数据质量差 | 匹配结果不准确 | 来源标记、更新时间、去重和下架检测 |
| 手机端无法自动控制招聘 App | 三端能力不一致 | 移动端采用深度链接、分享和用户确认 |
| 用户只关注投递数量 | 产品口碑下降 | 强调匹配质量、回复率和面试率 |

## 20. 首个版本的建议范围

首个可发布版本建议只包含：

1. Flutter Web；
2. Flutter Windows；
3. Android/iOS 基础客户端；
4. 用户登录和隐私授权；
5. 简历上传和解析；
6. AI 简历优化；
7. 多份简历版本；
8. 求职偏好；
9. 职位链接导入；
10. 工作台筛选；
11. 岗位匹配和去重；
12. 投递记录；
13. Chrome/Edge 辅助填表；
14. 用户确认最终投递；
15. 失败、验证码和登录失效提醒。

首个版本暂不承诺：

- 六个平台全部官方接入；
- 无人值守无限批量投递；
- 自动解决验证码；
- 自动完成测评和视频面试。

## 21. 上线前检查清单

- [ ] 用户协议和隐私政策完成；
- [ ] 简历删除和账号注销流程可用；
- [ ] 原始简历与 AI 版本分离；
- [ ] 投递前有清晰的确认页面；
- [ ] 重复投递拦截可用；
- [ ] 验证码出现时任务自动暂停；
- [ ] 不保存招聘平台密码；
- [ ] 所有投递事件可追溯；
- [ ] Web、Windows、Android、iOS 完成基础适配；
- [ ] 至少一个平台完成完整的辅助投递测试；
- [ ] 页面变化和适配器更新机制完成；
- [ ] AI 不得擅自编造个人经历；
- [ ] 日志中不得记录完整简历和密码；
- [ ] 有客服、申诉和故障回滚方案。

## 22. 最终建议

这个项目值得做，但产品成功的关键不是“自动投递多少份”，而是：

- 能否准确理解用户简历；
- 能否找到真正匹配的职位；
- 能否减少重复填写；
- 能否保证投递内容正确；
- 能否在多平台变化后持续稳定运行；
- 能否保护用户简历和个人信息；
- 能否提高回复率和面试率。

推荐的最终技术路线是：

```text
Flutter 多端客户端
+
NestJS/Go 业务后端
+
Python 简历解析和 AI 服务
+
PostgreSQL + Redis + 对象存储
+
Chrome/Edge 浏览器插件
+
Playwright 本地自动化助手
+
平台适配器和官方接口
```

最稳妥的产品路线是：

> **先做简历优化和岗位匹配，再做浏览器辅助填表，最后根据平台授权情况逐步开放自动投递。**

这样既能快速验证用户需求，也能避免一开始被平台接口、验证码、封号和隐私合规问题拖垮。

## 23. 开发前必须确认的事项

以下事项会直接影响产品范围、开发成本和上线时间，不能留到开发后期再决定：

### 23.1 首个用户群体

需要从以下方向中选择一个作为 MVP 的主要用户：

- 应届毕业生；
- Java、前端、测试等技术岗位；
- 教师和教育行业；
- 销售、运营和行政岗位；
- 有明确城市和薪资要求的专业人士。

首个用户群体越明确，简历模板、匹配规则和 AI 提示词越容易做准。

### 23.2 首个平台和投递方式

开发前必须确认：

- 首个平台是否允许浏览器辅助操作；
- 是否可以使用公开职位链接；
- 是否有开放平台或商务合作渠道；
- 是否允许浏览器扩展读取职位页面；
- 最终投递是否必须由用户确认；
- 验证码、短信验证和实名流程如何处理。

### 23.3 数据来源方式

每个平台都要明确标记数据来源：

```text
官方 API
官方合作数据源
用户主动导入的职位链接
用户当前页面的本地解析
其他来源（上线前必须完成合规审查）
```

没有明确来源和授权记录的职位数据，不应直接进入长期职位库。

### 23.4 AI 服务商选择

需要提前确定：

- 使用哪一家模型服务；
- 简历内容是否发送到第三方；
- 是否支持私有化部署；
- 是否保存请求和响应；
- 是否用于模型训练；
- 用户如何查看和撤回 AI 数据授权；
- 海外模型服务是否涉及跨境数据处理。

### 23.5 交付标准

MVP 不能只以“功能写出来”为完成标准。建议满足以下条件后才可以进入公开测试：

- 至少一个完整职位流程可以稳定运行；
- 简历解析错误可以由用户校正；
- AI 不会无提示地编造经历；
- 投递前能够看到全部关键字段；
- 验证码和登录失效会自动暂停任务；
- 重复投递可以被拦截；
- 用户可以删除自己的简历和投递记录；
- 所有平台操作都有可追溯日志；
- Web、桌面和移动端的权限边界已经在界面中说明。

### 23.6 版本管理建议

后续每次修改本文档时，建议在此处维护版本记录：

| 版本 | 日期 | 变更内容 |
|---|---|---|
| v1.0 | 2026-08-30 | 初版项目规划 |
| v1.1 | 2026-08-30 | 增加决策摘要、平台信息有效性说明、开发前确认事项 |
